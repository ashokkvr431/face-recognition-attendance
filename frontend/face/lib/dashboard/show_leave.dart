// show_leave.dart
import 'package:flutter/material.dart';

class ShowLeavePage extends StatelessWidget {
  const ShowLeavePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Show Leave")),
      body: const Center(child: Text("Show Leave Page")),
    );
  }
}
